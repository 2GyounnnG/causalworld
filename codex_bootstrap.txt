You are running unattended overnight.

Before any inspection, preflight, smoke test, or script execution, you MUST run these exact commands first and verify their outputs:

source ~/miniforge3/etc/profile.d/conda.sh
conda activate causalworld
cd /home/user/projects/causalworld
which python
python -c "import torch; print('torch', torch.__version__); print('cuda_available', torch.cuda.is_available()); print('torch_cuda', torch.version.cuda); print('device_count', torch.cuda.device_count()); print('device_name', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'no gpu')"

If and only if:
- which python == /home/user/miniforge3/envs/causalworld/bin/python
- torch.cuda.is_available() == True
- device_name contains NVIDIA GeForce RTX 5060 Ti

then proceed.

For every later Python command, use the activated causalworld environment. If there is any ambiguity, use:
/home/user/miniforge3/envs/causalworld/bin/python

Do not use the default base Python.
Do not stop just because base Python lacks torch.
Do not install anything.
Do not run any command that can prompt for input, password, host-key trust, or approval.
If a command would require interaction, skip it, log it in overnight_summary.md, and continue per the brief.

After this bootstrap succeeds, read and follow:
/home/user/projects/causalworld/overnight_brief.txt

Before real training, summarize the plan and the first commands you will run.
